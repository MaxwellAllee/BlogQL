import React from 'react';
import styled from 'styled-components';

const Slider = (props) => {
  const toggle = () => props.cb(true);
  const SwitchLabel = styled.label`
    position: relative;
    display: inline-block;
    width: 60px;
    height: 34px;
  `;
  const CheckBox = styled.input`

  `;
  return (

  )
};

export default Slider;
